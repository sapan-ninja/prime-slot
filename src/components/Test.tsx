import React from 'react'

export default function Test() {
    // const slotMachine = new SlotMachine(container, reels, callback, options);

    return (
        <div id="slot-machine" className="slot-machine"></div>

    )
}
